#include<stdio.h>
/**
 * main- entry point
 *
 * Return: 0 after execution
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%num", num);
	}

	printf("\n");

	return (0);
}
