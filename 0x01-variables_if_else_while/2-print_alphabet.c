#include <stdio.h>
/**
 * main- entry point
 *
 * Return: 0 after execution
 */
int main(void)
{
	char alpha;
	for(alpha = 'a'; alpha <= 'z'; z++)
	{
		putchar(alpha);
	}
	putchar('\n');
return (0);
}
