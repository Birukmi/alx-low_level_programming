#include <stdio.h>
/**
 * main- entry point'
 *
 * Return: 0 after successful execution
 */
int main(void)
{
	char n;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (n = 97; n <= 102; n++)
	{
		putchar(n);
	}
	
	putchar('\n');
return (0);
}
